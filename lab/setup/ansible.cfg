[defaults]
host_key_checking = False
retry_files_enabled = False
stdout_callback = yaml
inventory = ../inventory/workshop.yml

[privilege_escalation]
become = true
become_method = sudo
become_user = root

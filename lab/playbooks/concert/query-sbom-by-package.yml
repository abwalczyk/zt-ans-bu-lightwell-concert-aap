---
# Query Concert SBOM Inventory by Package
# Finds which applications still run an older version of a package that Lightwell
# has just published a remediated build for.
#
# Concert's Unique Value:
#   - Pre-indexed SBOM database (instant query vs. scanning 500+ repos)
#   - Enterprise-wide view (all apps, all teams)
#
# Required extra vars:
#   package_name: Package published by Lightwell (e.g., pyyaml)
#   package_version: New remediated version (e.g., 6.0.2)
#
# Returns (via set_stats):
#   concert_total_affected: Total number of applications on an older version
#   concert_affected_apps: List of those applications
#   concert_target_app_found: Boolean (whether target app is in the list)
#   concert_current_version: Current package version in target app

- name: Query Concert SBOM Inventory by package
  hosts: localhost
  gather_facts: false
  vars:
    # These arrive as job template extra_vars (see configure-aap.yml) and the
    # API key arrives as an injected env var from the Concert credential type.
    # An execution environment sees neither the control VM's env nor its files,
    # so a bare lookup('env', ...) here would always come back empty.
    concert_url: "{{ concert_api_url | default('', true) }}"
    concert_api_key: "{{ lookup('env', 'CONCERT_API_TOKEN') | default('', true) }}"
    concert_instance_id: "{{ concert_instance_id | default('0000-0000-0000-0000', true) }}"
    package_name: "{{ package_name }}"
    package_version: "{{ package_version }}"
    target_app: "{{ target_app | default('flask-api-app') }}"
    demo_mode: "{{ lab_demo_mode | default(true) | bool }}"

  tasks:
    # ── Live path ──────────────────────────────────────────────────────
    # Concert auth is "Authorization: C_API_KEY <key>" plus an InstanceId
    # header -- not Bearer. Base path is /concert/core/api/v2.
    - name: Query Concert inventory for applications using this package
      ansible.builtin.uri:
        url: "{{ concert_url }}/applications"
        method: GET
        headers:
          Authorization: "C_API_KEY {{ concert_api_key }}"
          InstanceId: "{{ concert_instance_id }}"
          Content-Type: "application/json"
        validate_certs: false
        status_code: 200
      register: concert_inventory_response
      when: not demo_mode

    - name: Parse Concert inventory response
      ansible.builtin.set_fact:
        inventory_data: "{{ concert_inventory_response.json }}"
      when: not demo_mode

    # ── Demo path ──────────────────────────────────────────────────────
    # Representative portfolio data matching the lab narrative. Shaped exactly
    # like the live response so the tasks below do not branch.
    - name: Build representative Concert inventory (demo mode)
      ansible.builtin.set_fact:
        inventory_data:
          package_name: "{{ package_name }}"
          total_applications: 537
          total_affected_applications: 12
          affected_applications:
            - name: "{{ target_app }}"
              version: v1.0.0
              package: "{{ package_name }}"
              current_version: "{{ current_version | default('5.4.1') }}"
              repository: "{{ app_repo_url | default('https://github.com/labuser/flask-api-app') }}"
              owner_team: platform-team
            - name: payments-api
              current_version: "5.3.1"
              owner_team: payments-team
            - name: reporting-worker
              current_version: "5.4.1"
              owner_team: data-team
      when: demo_mode

    - name: Note that these results are representative
      ansible.builtin.debug:
        msg: >-
          DEMO MODE: Concert was not queried. These SBOM figures are
          representative lab data. Set CONCERT_DEMO_MODE=false once this
          lab's SBOM is loaded into Concert.
      when: demo_mode

    - name: Find target application in affected list
      ansible.builtin.set_fact:
        target_app_data: "{{ inventory_data.affected_applications | selectattr('name', 'equalto', target_app) | first | default({}) }}"

    - name: Set workflow stats for SBOM inventory
      ansible.builtin.set_stats:
        data:
          concert_total_affected: "{{ inventory_data.total_affected_applications | default(0) }}"
          concert_affected_apps: "{{ inventory_data.affected_applications | default([]) }}"
          concert_target_app_found: "{{ target_app_data != {} }}"
          concert_package: "{{ package_name }}"
          concert_current_version: "{{ target_app_data.current_version | default('') }}"
          concert_owner_team: "{{ target_app_data.owner_team | default('') }}"
          concert_repository: "{{ target_app_data.repository | default('') }}"

    - name: Display Concert SBOM inventory results
      ansible.builtin.debug:
        msg: |
          ==========================================
          Concert SBOM Inventory Results
          ==========================================
          New Lightwell Package: {{ package_name }} {{ package_version }}

          PORTFOLIO-WIDE IMPACT:
          Applications on an older version: {{ inventory_data.total_affected_applications | default(0) }}
          (Concert scanned {{ inventory_data.total_applications | default('N/A') }} apps)

          TARGET APPLICATION:
          Name: {{ target_app }}
          Found in affected list: {{ 'YES' if target_app_data != {} else 'NO' }}
          {% if target_app_data != {} %}
          Current Version: {{ target_app_data.current_version }}
          Owner Team: {{ target_app_data.owner_team }}
          Repository: {{ target_app_data.repository }}
          {% endif %}

          OTHER AFFECTED APPS:
          {% for app in inventory_data.affected_applications | default([]) %}
          {% if app.name != target_app %}
          - {{ app.name }} ({{ package_name }} {{ app.current_version }})
          {% endif %}
          {% endfor %}
          ==========================================
